import styled from 'styled-components';

const Box = styled.div`
    display: flex;
`;

export const BoxH = styled(Box)`
    flex-direction: row;
`;

export const BoxV = styled(Box)`
    flex-direction: column;
`;
